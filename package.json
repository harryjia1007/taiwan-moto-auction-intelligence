{
  "name": "taiwan-moto-auction-intelligence",
  "description": "Evidence-preserving open-source intelligence for Taiwanese public motorcycle auctions",
  "private": true,
  "license": "Apache-2.0",
  "repository": {
    "type": "git",
    "url": "https://github.com/harryjia1007/taiwan-moto-auction-intelligence.git"
  },
  "packageManager": "pnpm@11.16.0",
  "engines": { "node": ">=24" },
  "scripts": {
    "dev": "pnpm --filter @tm-ai/web dev",
    "dev:start": "node scripts/local-web-server.mjs start",
    "dev:stop": "node scripts/local-web-server.mjs stop",
    "dev:restart": "node scripts/local-web-server.mjs restart",
    "dev:status": "node scripts/local-web-server.mjs status",
    "dev:logs": "node scripts/local-web-server.mjs logs",
    "build": "pnpm -r build",
    "lint": "pnpm -r lint",
    "test": "pnpm -r test",
    "test:web": "pnpm --filter @tm-ai/web test",
    "test:shared": "pnpm --filter @tm-ai/shared test",
    "test:e2e": "pnpm --filter @tm-ai/web test:e2e",
    "db:start": "supabase start",
    "db:stop": "supabase stop",
    "db:reset": "supabase db reset",
    "db:test": "supabase test db",
    "fixtures:media": "node scripts/cache-fixture-media.mjs",
    "ingest": "pnpm ingest:shwoo && pnpm ingest:pcc && pnpm ingest:judicial",
    "ingest:shwoo": "docker compose run --rm ingest sync --source shwoo",
    "ingest:pcc": "docker compose run --rm ingest sync --source pcc",
    "ingest:judicial": "docker compose run --rm ingest sync --source judicial",
    "ingest:health": "docker compose run --rm ingest healthcheck --source shwoo && docker compose run --rm ingest healthcheck --source pcc && docker compose run --rm ingest healthcheck --source judicial",
    "ingest:reprocess": "docker compose run --rm ingest reprocess --source shwoo",
    "test:ingest": "docker compose run --rm ingest-test"
  },
  "devDependencies": {
    "supabase": "^2.45.5"
  }
}
