# Deployment Configuration

The hosted database foundation is provisioned. The authenticated web deployment and scheduled-ingestion secrets remain gated on the owner email and deployment-provider configuration described below.

## Local managed server

`node scripts/local-web-server.mjs start` launches the local fixture dashboard as a detached, project-scoped process on `127.0.0.1:3000`. `status` verifies the stored PID, the exact Next.js command, and the `/api/health` response. Logs and runtime metadata stay under ignored `apps/web/.data/` files. The stop command refuses to signal a reused or unrelated PID.

The production build uses `.next-production`, while development uses `.next`, so running verification builds cannot overwrite a live development cache. The managed server is for local fixture use only; hosted deployment must use authenticated mode and real Supabase configuration.

## Supabase

The dedicated hosted project `taiwan-moto-auction` was created in `ap-southeast-1` on 2026-08-15 with project ref `hdxlhxqlkdipqkwisjyd`. The hosted migration ledger must match every committed migration before a release is promoted; the current release includes migrations through `202608230009_public_contact_identifier_constraint.sql`. Migrations `008` and `009` add a production-safe nine-source registry bootstrap and a database-level fail-closed check for Taiwan IDs, phone numbers and email addresses in the anonymous projection. Post-migration verification checks for zero personal contact identifiers or labelled private residential/contact addresses in public text, zero out-of-policy public plates, zero public photo URLs, a private `raw-artifacts` bucket, RLS on every public table, anonymous SELECT access only to the sanitized live-listing and source-health projections, no anonymous access to operational tables, no destructive `service_role` table privileges, and no enabled central Judicial endpoint.

`supabase/seed.sql` is development/test-only and must not be applied to the hosted project: it contains the local owner placeholder and sanitized synthetic fixtures. Production registry metadata is created by migration `202608230008`; real records are added only by scheduled or policy-approved manual publishers.

Run `pnpm run doctor` before bootstrapping. The Supabase CLI packages PostgreSQL, Auth, Storage, local email, and the pgTAP runner as a Docker-compatible local stack; installing the JavaScript CLI alone does not create those services. Docker Desktop is the default, while OrbStack, Podman, and Colima are documented compatible alternatives.

1. Link the repository to project ref `hdxlhxqlkdipqkwisjyd` when CLI access is configured.
2. Update `app_settings.owner_email` to the same lowercase value as `OWNER_EMAIL` before enabling login.
3. Keep the `raw-artifacts` bucket private.
4. Add the production callback URL to Auth redirect URLs.
5. Create and verify the owner account, then disable public sign-up in hosted Auth. Confirm that an arbitrary non-owner email cannot create an account or read operational data directly through Supabase.

Private artifact retention is not yet automatic in the hosted environment. Each raw artifact has a calculated 12-month-or-later deadline, but the only deletion path is the operator-run `python -m ingest retention --source <adapter>` dry-run followed by an explicitly reviewed `--execute` with a privileged `DATABASE_URL` and private Storage credentials. The hosted ingestion workflow has no such database connection or retention job. Do not report an artifact as deleted until its Storage byte deletion and `artifact_tombstones` record have both been verified; establishing a scheduled, audited retention worker remains a release operations item.

## Vercel

Use `apps/web` through the root `vercel.json`. Configure `NEXT_PUBLIC_SUPABASE_URL`, `NEXT_PUBLIC_SUPABASE_ANON_KEY`, and `OWNER_EMAIL`. Never configure `TM_FIXTURE_MODE=true` in production. The service-role key is not a frontend variable.

## GitHub Actions

Scheduled public ingestion requires encrypted `SUPABASE_URL` and `SUPABASE_SECRET_KEY` repository secrets; the legacy `SUPABASE_SERVICE_ROLE_KEY` remains a fallback during migration. The GitHub-hosted workflow validates both before making a source request, then runs MOJ centralized auctions, PCC official open data, Customs four-office HTML, the 13 Administrative Enforcement branch CMS sources, and the Judicial Yuan main-site `judicial_notices` supplement at 09:30 and 21:30 Asia/Taipei. PCC itself updates on working days; repeated checks reduce detection delay without implying a second upstream refresh. Every Administrative Enforcement branch rechecks its own robots and sitemap. The Judicial supplement independently rechecks `www.judicial.gov.tw/robots.txt`, searches only the recent main-site public notice range, and never contacts the separate `aomp109` application or downloads attachments. Taipei Shwoo repeatedly completed health checks but timed out on every full discovery attempt from GitHub-hosted runner addresses on 2026-08-17; it is deliberately absent from the GitHub Actions matrix and remains a separately operated Taiwan-network batch source. It therefore does **not** currently receive the hosted twice-daily freshness guarantee. Central Judicial discovery is also absent from the matrix: its manifest adapter rejects a run without a human-reviewed manifest before constructing a source request, and registry aomp109 endpoints are disabled manual references. The workflow never logs in, bids, submits CAPTCHA, or treats a partial/zero run as proof that no cases exist.

On the same twice-daily schedule, an independent, secret-free `catalog-watch` job checks the official [data.gov.tw dataset 6564](https://data.gov.tw/dataset/6564) daily JSON catalog for recently published government/administrative-enforcement/judicial vehicle-auction datasets. It runs independently so a source outage cannot suppress catalog discovery, does not query auction cases, and never writes Supabase. No candidate is a successful source integration: candidates produce an exit-0 warning containing only the dataset ID and official metadata URL, while network, redirect, MIME, size and catalog validation failures return nonzero. The default 14-day window intentionally repeats a candidate until it ages out or an operator reviews it; accepting one requires a separate policy, adapter, fixture and source-registry change. See [the catalog watcher specification](ingestion/data-gov-catalog.md).

An adapter being healthy does not prove the hosted schedule is operating. Release verification must check the latest GitHub Actions conclusion and confirm a matching `sync_runs` row, nonzero source metrics when official results exist, artifact metadata, snapshots and `last_successful_at`. Missing repository secrets are a deployment blocker, not a source outage.

As of 2026-08-23 the repository Secrets `SUPABASE_URL` and `SUPABASE_SECRET_KEY` exist. The latest legacy scheduled run published MOJ (29 parsed rows with three visibly partial detail failures), PCC (5/5) and Customs (1/1); the Administrative Enforcement branch job failed because no branch was reachable from the US runner. This is evidence for those individual runs only. The current release matrix, migrations `008`–`009`, `judicial_notices` job and `public_source_health` projection are not hosted-operational until this branch is pushed, the hosted migration ledger is updated and a new workflow run is verified.

### Production audit, 2026-09-25

The production page returned HTTP 200 and its anonymous, sanitized listing endpoint returned 117 rows: 47 PCC, 29 MOJ centralized auctions, 2 Customs, 25 Shwoo, and 14 historical Judicial records. The latest successful listing timestamps were 2026-09-25 for PCC/MOJ/Customs, 2026-08-27 for Shwoo, and 2026-08-14 for Judicial. A row remaining in the public table is **not** proof of current availability. The last ten scheduled workflow runs (2026-09-20 through 2026-09-25) were overall failures: the MOJ/PCC/Customs jobs succeeded on 2026-09-25, while the branch CMS job failed because none of its 13 preflights completed. The official branch sites now redirect `/robots.txt` to same-host `/robots`; the deployed adapter rejects that redirect before loading a policy. This is a diagnosis, not a claim that a repaired adapter has been deployed or that the branch cases were synced.

The hosted `public_source_health` endpoint returned HTTP 404 (`PGRST205`) on 2026-09-25, so the new source-health migration has not been verified in production. The expanded workflow, Judicial supplemental source, source-health projection, and any local adapter repair remain release candidates until a reviewed commit, migration check, production sync, and post-deployment smoke test are complete. Do not describe the project as providing complete nationwide coverage or twice-daily updates for Shwoo, Judicial, or branch CMS while these conditions hold. The public UI should surface per-source last-sync freshness instead of making a blanket schedule claim.

The Administrative Enforcement central search remains excluded from unattended schedules because its discovery form requires a human-completed CAPTCHA. A direct result GET without a CAPTCHA session is not used as an automation path. After a human performs the official `汽機車` search, either save every result page under the ignored `.data/moj-enforcement-export/` directory or save validated detail URLs in `.data/moj-enforcement-manifest.json`. The importer parses exported HTML offline, then reads only robots-allowed same-host details; it never fetches central Query pages, PDF bytes, or JPG/GIF images. Official PDF full text remains a verified outbound link. This is separate from the scheduled `moj_enforcement_cms` branch-announcement source.

The source dashboard must retain truthful readiness states. `PARTIAL` means the adapter is usable under its documented limits, not that nationwide discovery is complete. Only promote a source to `ACTIVE` after a successful real database sync, raw-artifact persistence, and an operator review of source metrics.

## Public portfolio boundary

The portfolio page at `harryjia.com/projects/taiwan-moto-auction` reads only `public_live_motorcycle_listings` with a publishable key. Operational tables, `/motorcycles`, `/sources`, private APIs, cached artifacts, evidence and full snapshots remain owner-only. Public plates always hide the final two or three characters; a plate is not published when the official end time is unknown and is cleared 30 days after a verified end time. Engine/frame/VIN identifiers never enter the projection. Taiwan IDs, phone/email values, role-labelled names, and labelled private residential/contact addresses are redacted from public text; official vehicle viewing or storage locations remain available when they are not presented as a person's address. A personal-data-bearing official URL falls back to its source origin and such document URLs are removed rather than rewritten. The `20260821` public-data backfills, `202608230006` private-address backfill and `202608230009` contact-identifier gate repair or deactivate unsafe existing rows, while every hosted publisher run repeats the same idempotent safety transformations before it may report success. Private normalized history, snapshots, evidence and artifacts are unchanged.

No current source has an approved anonymous-photo redistribution decision. The public projection therefore stores an empty `photo_urls` array and renders a compact no-photo card. Owner-only pages may continue to display privately cached official images. Public photos can be enabled only through a reviewed source-specific rights decision plus an exact-host HTTPS:443 allowlist; private Storage paths and service-role credentials never enter the projection.

The separate `database-tests.yml` workflow needs no project secrets. It starts an isolated Supabase stack on the GitHub runner, replays every migration and the development/test seed, runs the committed 187-assertion pgTAP suite, reports status on failure, and always stops the stack. Seed assertions address fixed sanitized fixture identifiers instead of global row counts. This supplements local testing; it does not deploy, seed or create a hosted Supabase project.
